SHOW TABLES;

CREATE SCHEMA RAW_LAYER;

CREATE TABLE CUSTOMER (
ID   INT,
NAME VARCHAR
);
DROP TABLE CUSTOMER;
CREATE TEMP TABLE CUSTOMER (
ID   INT,
NAME VARCHAR
);

CREATE TRANSIENT TABLE EMPLOYEE(
EID INT,
ENAME VARCHAR
);

CREATE TEMP TABLE ORDERS(
OID INT,
ONAME VARCHAR
);

CREATE HYBRID TABLE HCUSTOMER(
HID INT PRIMARY KEY,
HNAME VARCHAR
);

